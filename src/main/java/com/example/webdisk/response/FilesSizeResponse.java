package com.example.webdisk.response;

/**
 * A record that represents the response containing the size of files.
 *
 * @param size the size of the files
 */
public record FilesSizeResponse(int size) { }
